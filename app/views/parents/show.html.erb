<p>
  <strong>User:</strong>
  <%= @parent.user.name %>
</p>

<%= link_to t('edit'), edit_parent_path(@parent), class: 'btn btn-sm btn-success' %>
<%= link_to t('back'), parents_path, class: 'btn btn-sm btn-danger' %>
