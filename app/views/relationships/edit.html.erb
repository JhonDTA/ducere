<% provide(:title, t('edit_resource', resource: t('activerecord.models.relationship'))) %>

<div class="col-md-6 offset-md-3">
  <%= render 'form', relationship: @relationship %>

  <%= link_to 'Show', @relationship %> |
  <%= link_to 'Back', relationships_path %>
</div>
