<% provide(:title, t('new_resource', resource: t('activerecord.models.academic_cycle'))) %>
<% provide(:card_title, t('activerecord.models.academic_cycle')) %>

<%= render 'shared/cards/form_card' do %>
  <%= render 'form', academic_cycle: @academic_cycle %>
  <% content_for :card_footer do %>
    <%= link_to academic_cycles_path, class: 'btn btn-sm btn-danger' do %>
      <%= icon('fas', 'undo-alt') %> <%= t('back') %>
    <% end %>
  <% end %>
<% end %>
