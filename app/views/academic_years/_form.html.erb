<%= form_with(model: academic_year, local: true) do |form| %>
  <% if academic_year.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(academic_year.errors.count, "error") %> prohibited this academic_year from being saved:</h2>

      <ul>
        <% academic_year.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :start %>
    <%= form.date_select :start, class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :finish %>
    <%= form.date_select :finish, class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :campu_id %>
    <%= form.collection_select(:campu_id, Campu.all, :id, :name, class: 'form-control') %>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>
