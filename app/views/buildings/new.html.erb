<% provide(:title, t('new_resource', resource: t('activerecord.models.building'))) %>

<div class="col-md-6 offset-md-3">
  <%= render 'form', building: @building %>

  <%= link_to 'Back', buildings_path %>
</div>
