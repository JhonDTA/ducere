<% provide(:title, @status.name) %>
<% provide(:link, link_to(@status.name, @status)) %>

<p>
  <strong>Code:</strong>
  <%= @status.code %>
</p>

<p>
  <strong>Name:</strong>
  <%= @status.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @status.description %>
</p>

<%= link_to 'Edit', edit_status_path(@status) %> |
<%= link_to 'Back', statuses_path %>
