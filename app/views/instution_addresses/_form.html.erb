<%= form_with(model: instution_address, local: true) do |form| %>
  <% if instution_address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(instution_address.errors.count, "error") %> prohibited this instution_address from being saved:</h2>

      <ul>
        <% instution_address.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :institution_id %>
    <%= form.text_field :institution_id %>
  </div>

  <div class="field">
    <%= form.label :address_id %>
    <%= form.text_field :address_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
