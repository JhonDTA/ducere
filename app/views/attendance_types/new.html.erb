<% provide(:title, t('new_resource', resource: t('activerecord.models.attendance_type'))) %>
<% provide :card_title, t('activerecord.models.attendance_type') %>

<%= render 'shared/forms/form_card' do %>
  <%= render 'form', attendance_type: @attendance_type %>
  <% content_for :back_button do %>
    <%= link_to t('back'), attendance_types_path, class: 'btn btn-sm btn-danger' %>
  <% end %>
<% end %>
