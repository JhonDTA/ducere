<% provide :title, t('edit_resource', resource: t('activerecord.models.address')) %>
<% provide :card_title, t('activerecord.models.address') %>

<%= render 'shared/cards/form_card' do %>
  <%= render 'form', address: @address %>
  <% content_for :card_footer do %>
    <%= link_to @address, class: 'btn btn-sm btn-info' do %>
      <%= icon('fas', 'eye') %> <%= t('show') %>
    <% end %>
    <%= link_to addresses_path, class: 'btn btn-sm btn-danger' do %>
      <%= icon('fas', 'undo-alt') %> <%= t('back') %>
    <% end %>
  <% end %>
<% end %>
