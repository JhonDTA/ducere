<% provide :title, @grade.name %>
<% provide :card_title, t('activerecord.models.grade') %>
<% content_for :card_footer do %>
  <%= link_to t('edit'), edit_grade_path(@grade), class: 'btn btn-sm btn-success' %>
  <%= link_to t('back'), grades_path, class: 'btn btn-sm btn-danger' %>
<% end %>

<%= render 'shared/cards/show' do %>
  <p>
    <strong>Code:</strong>
    <%= @grade.code %>
  </p>

  <p>
    <strong>Name:</strong>
    <%= @grade.name %>
  </p>

  <p>
    <strong>Description:</strong>
    <%= @grade.description %>
  </p>

  <p>
    <strong>Status:</strong>
    <%= @grade.status.name %>
  </p>
<% end %>
