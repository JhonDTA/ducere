<%= form_with(model: course_homework, local: true) do |form| %>
  <% if course_homework.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_homework.errors.count, "error") %> prohibited this course_homework from being saved:</h2>

      <ul>
        <% course_homework.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :course_evaluation_id %>
    <%= form.text_field :course_evaluation_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
