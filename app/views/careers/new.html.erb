<% provide(:title, t('new_resource', resource: t('activerecord.models.career'))) %>

<div class="col-md-6 offset-md-3">
  <%= render 'form', career: @career %>

  <%= link_to t('back'), careers_path %>
</div>
