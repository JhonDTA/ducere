<% provide(:title, t('edit_resource', resource: t('activerecord.models.career'))) %>
<% provide :card_title, t('activerecord.models.career') %>

<%= render 'shared/cards/form_card' do %>
  <%= render 'form', career: @career %>
  <% content_for :back_button do %>
    <%= link_to t('show'), @career, class: 'btn btn-sm btn-info' %>
    <%= link_to 'Back', careers_path %>
  <% end %>
<% end %>
