<% provide(:title, t('new_resource', resource: t('activerecord.models.cycle_modality'))) %>
<% provide :card_title, t('activerecord.models.cycle_modality') %>

<%= render 'shared/forms/form_card' do %>
  <%= render 'form', cycle_modality: @cycle_modality %>
  <% content_for :back_button do %>
    <%= link_to t('back'), cycle_modalities_path, class: 'btn btn-sm btn-danger' %>
  <% end %>
<% end %>
