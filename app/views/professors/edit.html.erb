<% provide(:title, t('edit_resource', resource: t('activerecord.models.professor'))) %>
<% provide :card_title, t('activerecord.models.professor') %>

<%= render 'shared/cards/form_card' do %>
  <%= render 'form', professor: @professor %>
  <% content_for :card_footer do %>
    <%= link_to @professor, class: 'btn btn-sm btn-info' do %>
      <%= icon('fas', 'eye') %> <%= t('show') %>
    <% end %>
    <%= link_to professors_path, class: 'btn btn-sm btn-danger' do %>
      <%= icon('fas', 'undo-alt') %> <%= t('back') %>
    <% end %>
  <% end %>
<% end %>
