<%= form_with(model: institution_address, local: true) do |form| %>
  <% if institution_address.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(institution_address.errors.count, "error") %> prohibited this institution_address from being saved:</h2>

      <ul>
        <% institution_address.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :institution_id %>
    <%= form.text_field :institution_id %>
  </div>

  <div class="field">
    <%= form.label :address_id %>
    <%= form.text_field :address_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
