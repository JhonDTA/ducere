<% provide(:title, t('new_resource', resource: t('activerecord.models.grade_course'))) %>
<% provide :card_title, t('activerecord.models.grade_course') %>

<%= render 'shared/cards/form_card' do %>
  <%= render 'form', grade_course: @grade_course %>
  <% content_for :card_footer do %>
    <%= link_to grade_courses_path, class: 'btn btn-sm btn-danger' do %>
      <%= icon('fas', 'undo-alt') %> <%= t('back') %>
    <% end %>
  <% end %>
<% end %>
