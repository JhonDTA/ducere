<p>
  <strong>Cycle modality:</strong>
  <%= @cycle_turn.cycle_modality.name %>
</p>

<p>
  <strong>Turn:</strong>
  <%= @cycle_turn.turn.name %>
</p>

<%= link_to t('edit'), edit_cycle_turn_path(@cycle_turn), class: 'btn btn-sm btn-success' %>
<%= link_to t('back'), cycle_turns_path, class: 'btn btn-sm btn-danger' %>
