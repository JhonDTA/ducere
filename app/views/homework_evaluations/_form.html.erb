<%= form_with(model: homework_evaluation, local: true) do |form| %>
  <% if homework_evaluation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(homework_evaluation.errors.count, "error") %> prohibited
        this homework_evaluation from being saved:</h2>

      <ul>
        <% homework_evaluation.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :student_homework_id %>
    <%= form.collection_select :student_homework_id, StudentHomework.all, :id, :name,
                               class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :mark %>
    <%= form.number_field :mark, class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :observations %>
    <%= form.text_area :observations, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-primary form-control' %>
  </div>
<% end %>
