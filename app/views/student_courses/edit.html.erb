<% provide(:title, t('edit_resource', resource: t('activerecord.models.student_course'))) %>

<%= render 'form', student_course: @student_course %>

<%= link_to 'Show', @student_course %> |
<%= link_to 'Back', student_courses_path %>
