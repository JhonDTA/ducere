<% provide(:title, t('new_resource', resource: t('activerecord.models.student_course'))) %>
<% provide :card_title, t('activerecord.models.student_course') %>

<%= render 'shared/cards/form_card' do %>
  <%= render 'form', student_course: @student_course %>
  <% content_for :card_footer do %>
    <%= link_to student_courses_path, class: 'btn btn-sm btn-danger' do %>
      <%= icon('fas', 'undo-alt') %> <%= t('back') %>
    <% end %>
  <% end %>
<% end %>
