<p>
  <strong>Student:</strong>
  <%= @student_course.student.name %>
</p>

<p>
  <strong>Course evaluation:</strong>
  <%= @student_course.course_evaluation.name %>
</p>

<%= link_to t('edit'), edit_student_course_path(@student_course) %> |
<%= link_to t('back'), student_courses_path %>
