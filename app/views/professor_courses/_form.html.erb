<%= form_with(model: professor_course, local: true) do |form| %>
  <% if professor_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(professor_course.errors.count, "error") %> prohibited this professor_course from being saved:</h2>

      <ul>
        <% professor_course.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :professor_id %>
    <%= form.text_field :professor_id %>
  </div>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.text_field :course_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
