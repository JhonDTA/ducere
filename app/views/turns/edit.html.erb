<% provide(:title, t('edit_resource', resource: t('activerecord.models.turn'))) %>

<div class="col-md-6 offset-md-3">
  <%= render 'form', turn: @turn %>

  <%= link_to 'Show', @turn %> |
  <%= link_to 'Back', turns_path %>
</div>
