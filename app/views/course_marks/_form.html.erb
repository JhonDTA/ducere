<%= form_with(model: course_mark, local: true) do |form| %>
  <% if course_mark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_mark.errors.count, "error") %> prohibited this course_mark from being saved:</h2>

      <ul>
        <% course_mark.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :student_course_id %>
    <%= form.text_field :student_course_id %>
  </div>

  <div class="field">
    <%= form.label :mark %>
    <%= form.number_field :mark %>
  </div>

  <div class="field">
    <%= form.label :attendance %>
    <%= form.number_field :attendance %>
  </div>

  <div class="field">
    <%= form.label :observations %>
    <%= form.text_area :observations %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
