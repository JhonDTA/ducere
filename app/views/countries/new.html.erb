<% provide(:title, t('new_resource', resource: t('activerecord.models.country'))) %>
<% provide :card_title, t('activerecord.models.country') %>

<%= render 'shared/forms/form_card' do %>
  <%= render 'form', country: @country %>
  <% content_for :back_button do %>
    <%= link_to t('back'), countries_path %>
  <% end %>
<% end %>
