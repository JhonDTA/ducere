<% provide(:title, t('editing_object', resource: t('activerecord.models.country'))) %>



<%= render 'form', country: @country %>

<%= link_to t('show'), @country %> |
<%= link_to t('back'), countries_path %>
