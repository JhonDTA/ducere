<%= form_with(model: turn_evaluation, local: true) do |form| %>
  <% if turn_evaluation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(turn_evaluation.errors.count, "error") %> prohibited this turn_evaluation from being saved:</h2>

      <ul>
        <% turn_evaluation.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cycle_turn_id %>
    <%= form.text_field :cycle_turn_id, class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :evaluation_period_id %>
    <%= form.text_field :evaluation_period_id, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
