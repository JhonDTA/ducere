<table>
  <thead>
  <tr>
    <th>Code</th>
    <th>Name</th>
    <th>Description</th>
    <th>Institution</th>
    <th>Status</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @campuses.each do |campus| %>
    <tr>
      <td><%= campus.code %></td>
      <td><%= campus.name %></td>
      <td><%= campus.description %></td>
      <td><%= campus.institution.name %></td>
      <td><%= campus.status.name %></td>
      <td><%= link_to t('show'), campus %></td>
      <td><%= link_to t('edit'), edit_campus_path(campus) %></td>
      <td><%= link_to t('destroy'), campus, method: :delete, data: { confirm: t('you_sure') } %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Campus', new_campus_path %>
<%= will_paginate @campuses %>
